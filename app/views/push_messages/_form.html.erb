<%= form_with(model: push_message, local: true) do |form| %>
  <% if push_message.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(push_message.errors.count, "error") %> prohibited this push_message from being saved:</h2>

      <ul>
      <% push_message.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :a %>
    <%= form.text_field :a %>
  </div>

  <div class="field">
    <%= form.label :b %>
    <%= form.text_field :b %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
