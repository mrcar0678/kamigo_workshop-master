<%= form_with(model: care_you, local: true) do |form| %>
  <% if care_you.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(care_you.errors.count, "error") %> prohibited this care_you from being saved:</h2>

      <ul>
      <% care_you.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :a1 %>
    <%= form.text_field :a1 %>
  </div>

  <div class="field">
    <%= form.label :b1 %>
    <%= form.text_field :b1 %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
