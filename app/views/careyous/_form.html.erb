<%= form_with(model: careyou, local: true) do |form| %>
  <% if careyou.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(careyou.errors.count, "error") %> prohibited this careyou from being saved:</h2>

      <ul>
      <% careyou.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :id %>
    <%= form.text_field :id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
